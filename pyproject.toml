[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "port-scanner"
version = "0.1.0"
description = "A simple TCP port scanner written in Python"
requires-python = ">=3.12"

[tool.setuptools.packages.find]
where = ["src"]